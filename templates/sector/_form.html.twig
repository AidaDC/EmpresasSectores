
<div class="container">
    <div class="row">
       <div class="col-xs-12">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button id="boton_borrarS" class="btn btn-primary boton_borrar">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
       </div>
    </div>
</div>