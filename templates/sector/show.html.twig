{% extends 'base.html.twig' %}

{% block title %}Sector{% endblock %}

{% block body %}
    <h1>Sector</h1>

    <table  id="example" class="table table-striped table-bordered" style="width:100%">
        <tbody>
            <tr>
                <th>Identificador</th>
                <td>{{ sector.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sector.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="boton_crear" href="{{ path('sector') }}">back to list</a>

    <a  class="boton_crear" href="{{ path('sector_edit', {'id': sector.id}) }}">edit</a>

    {{ include('sector/_delete_form.html.twig') }}
{% endblock %}
